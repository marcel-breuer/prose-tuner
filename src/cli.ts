/**
 * Command-line wiring is intentionally introduced with the CLI milestone.
 *
 * Keeping the module boundary in place lets later work expose the same core API
 * to both the Agent Skill and the executable without changing the source layout.
 */
export {};
